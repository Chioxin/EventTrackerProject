import { SortByCarPipe } from './sort-by-car.pipe';

describe('SortByCarPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCarPipe();
    expect(pipe).toBeTruthy();
  });
});
